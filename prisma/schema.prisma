generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = ".postgresql://srnandex:wDOHxp1GckX7@ep-little-tree-13692289.us-east-2.aws.neon.tech/db-api-talk-about-play?sslmode=require"
}

model User {
  id         Int      @id @default(autoincrement())
  email      String   @unique
  name       String
  password   String
  Chat       Chat[]
}

model Chat {
  id         Int      @id @default(autoincrement())
  content    String
  userId     Int
  authorName String
  created_at DateTime @default(now())
  author     User     @relation(fields: [userId], references: [id])
}
